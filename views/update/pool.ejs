<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
        <h1><%= title %></h1>
        <ul>
            <% errors.forEach((item) => { %>
                <li><%= item.msg %></li>
            <% }) %>
        </ul>
        <form action="/update/pool" method="POST">
            <label for="oldName"> Change pool </label>
            <select name="oldName" id="oldName">
                <%- include('../partials/options', {table: pools}) %>
            </select>
            <label for="newName"> to </label>
            <input type="text" id="newName" name="newName">
            <button type="submit">Submit</button>
        </form>
        <a href="/update">Back to update page</a>
</body>
</html>
